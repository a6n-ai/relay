## Summary

-

## Test plan

- [ ] `pnpm typecheck` and `pnpm test`
- [ ] If you changed HTTP routes, OpenAPI in `apps/relay/lib/v1/openapi.ts` matches the live handlers
- [ ] If you changed operator UI, check the related console pages (not only the one you edited)
